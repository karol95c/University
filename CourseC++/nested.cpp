#include <iostream>
#include "ring.h"

int main()
{

    // Ring<std::string> textRing(3);

    // Ring<std::string>::iterator it = textRing.begin();

    // textRing.push("One");
    // textRing.push("Two");
    // textRing.push("Three");
    // textRing.push("Four");

    // for (int i = 0; i < textRing.size(); ++i)
    // {
    //     std::cout << textRing.get(i) << std::endl;
    // }
    // for (auto it = textRing.begin(); it != textRing.end(); ++it)
    // {
    //     std::cout << *it << std::endl;
    // }

    // for (auto x : textRing)
    // {
    //     std::cout << x << std::endl; 
    // }

    // Ring<int> initRing{1, 2, 3, 4, 5};

    // for (auto x : initRing)
    // {
    //     std::cout << x << std::endl; 
    // }
 
    // initRing.push({10, 20, 30, 40, 50, 60});
    // // initRing.push(7);

    // for (auto x : initRing)
    // {
    //     std::cout << x << std::endl; 
    // }
    return 0;

}